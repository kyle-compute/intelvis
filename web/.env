# .env - The source of truth for secrets and config

# --- DATABASE CREDENTIALS ---
# These are the variables the TimescaleDB Docker image looks for.
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=mydb

# --- BACKEND SECRETS ---
# This is the full connection string the backend will use to connect to the DB.
# Note that it uses the variables defined above.
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public
JWT_SECRET=a-very-strong-and-secret-key-that-you-should-change

# --- FRONTEND CONFIG ---
# This is passed to the frontend at build time.
NEXT_PUBLIC_API_URL=https://api.intelvis.ai