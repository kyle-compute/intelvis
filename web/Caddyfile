# Caddyfile - THE FINAL, SIMPLIFIED VERSION

intelvis.ai {
    reverse_proxy frontend:3000
}

api.intelvis.ai {
    # Simple, clean proxy pass. The backend handles the full /api path.
    reverse_proxy backend:3001
}